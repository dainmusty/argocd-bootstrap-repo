# argocd-bootstrap-repo
A GitOps bootstrap repository that serves as the single source of truth for the cluster. Applied once, it enables ArgoCD to automatically manage, sync, and deploy all workload applications—each sourced from its own repository—using the App-of-Apps pattern.
